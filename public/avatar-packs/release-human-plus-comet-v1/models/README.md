This directory previously pointed at a machine-local avatar asset symlink.

For portable builds, Clawatar now relies on remote/default model URLs (see public/clawatar.config.json) or locally dropped VRM files instead of requiring this repo-local symlink target.
